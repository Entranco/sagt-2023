\begin{frame}[t]
    \frametitle{Known Results}

    \begin{itemize}
		\item Parameter: $k$
	\end{itemize}

	\vspace{0.5em}
    \input{figures/k_table.tex}
\end{frame}

\begin{frame}[t]
    \frametitle{Reducing From Set Cover to Gerrymandering}
    \problembox{Set Cover}
    {A set of elements $\Un = \{e_1, \dots, e_n\}$, a family of sets $\F = \{S_1, \dots, S_m\}$, and an integer $t \in \mathbb{N}$.}
    {Is there a subset $X \subseteq \F$ such that $|X| \leq t$ and $\bigcup_{S \in X} S = \Un$?}
    
    \scov is W[2]-Hard with respect to $t$, size of our cover.

    We want to show that \gm is W[2]-Hard with respect to $k$ districts.

    \vspace{1.0cm}
    To do this, we must reduce \scov to \gm.
    \begin{itemize}
        \item Our preferred candidate wins $\leftrightarrow$ we have a set cover
        \item Additionally, $k$ must be bounded by some function of $t$
    \end{itemize}
\end{frame}

\begin{frame}[t]
	\frametitle{W[2]-Hardness in Trees}

	\begin{itemize}
		\item
		$S_1 = \{a, b\}$ \hspace{1em}
		$S_2 = \{b\}$ \hspace{1em}
		$S_3 = \{c\}$ \hspace{1em}
		$S_4 = \{a, c\}$
	\end{itemize}

	\begin{center}
		\resizebox{\textwidth}{!}{
			\input{figures/hardness.tikz}
		}
	\end{center}

	\vspace{0.5em}

	\begin{itemize}
		\item
		\only<1> {
			Consider the above set-cover instance with frequency 2
		}
		\only<2>{
			Add blue vertices $c$ and $r$, blue is the preferred candidate
		}

		\only<3>{
			$\forall S_i \in \mathcal{F}$, add $s_i$ with a unique color ($+m$ colors)
		}

		\only<4-6>{
			$\forall e \in \mathcal{U}$, add $d$ vertices with a unique color ($+n$ colors)
		}

		\only<7>{
			Observation: $c$ must be in its own district, $r$ must win its district
		}

		\only<8-9>{
			Add weights so $r$ can only win if a set cover is chosen
		}

		\only<10>{
			Observation: other leaves should not be in their own district
		}

		\only<11>{
			Set cover $\iff$ remove copy of each element from root district \looseness=-1
		}
	\end{itemize}
\end{frame}